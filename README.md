# Mini Lakehouse

A transactional Parquet-based mini lakehouse (Delta/Iceberg-inspired) with streaming ingestion and an optimized SQL engine.

## Prerequisites
- Rust (stable) via rustup

Install formatting + clippy:
```bash
rustup component add rustfmt clippy
